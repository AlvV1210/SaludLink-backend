# Copia a .env solo si tu entorno carga variables (Spring Boot usa por defecto application.properties).
# Valores de ejemplo alineados con docker-compose.yml del proyecto.

# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/saludlink_db
# SPRING_DATASOURCE_USERNAME=postgres
# SPRING_DATASOURCE_PASSWORD=admin123

# jwt.secret=definir-en-produccion-min-256-bits
# jwt.expiration=86400000
